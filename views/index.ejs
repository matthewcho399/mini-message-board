<html>
  <body>
    <h1><%= title%></h1>

    <div>
      <% messages.map((message) => { %>
      <h3><%= message.user%></h3>
      <p><%= message.text%></p>
      <p><%= message.added%></p>
      <% }) %>
    </div>
  </body>
</html>
